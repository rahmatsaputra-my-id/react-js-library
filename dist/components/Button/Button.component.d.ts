/// <reference types="react" />
import { IButtonProps } from './Button.types';
declare const Button: ({ backgroundColor, bold, borderRadius, bottom, center, disabled, isLoading, label, left, onPress, padding, right, size, style, top, transparent, outlineColor, ...props }: IButtonProps) => JSX.Element;
export default Button;
